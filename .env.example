# 复制为 .env 并填入真实值（.env 会被 git 忽略，勿提交）

# ---- 生成层（OpenAI 兼容协议，默认 DeepSeek，可换成 OpenAI / 智谱 / 通义等）----
LLM_API_KEY=
LLM_API_BASE=https://api.deepseek.com
LLM_MODEL=deepseek-chat

# ---- 可选调参 ----
# EMBED_MODEL=BAAI/bge-m3
# TOP_K=4
# CHUNK_SIZE=600
# CHUNK_OVERLAP=100
